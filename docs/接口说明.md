# Docker Registry HTTP API V2 接口实现说明# Docker Registry HTTP API V2 接口实现说明# Docker Registry HTTP API V2 接口实现说明# Manifest

本文档详细说明了项目中实现的所有 Docker Registry HTTP API V2 接口，包括设计思路、实现细节和权衡决策。

## 目录

本文档详细说明了项目中实现的所有 Docker Registry HTTP API V2 接口，包括设计思路、实现细节和权衡决策。

- [基础 API](#基础-api)
- [Manifest 操作接口](#manifest-操作接口)
- [Blob 操作接口](#blob-操作接口)## 目录## 目录   * 实现了通过 `tag` 或 `digest` 拉取 Manifest
- [存储层实现](#存储层实现)
- [实现权衡与限制](#实现权衡与限制)

## 基础 API- [Manifest 操作接口](#manifest-操作接口)

### `GET /v2/`- [Blob 操作接口](#blob-操作接口)- [Manifest 操作接口](#manifest-操作接口)

- 返回了所有必需的头信息 (`Content-Type`, `Content-Length`, `Docker-Content-Digest`)

**功能**: API 版本检查和兼容性验证  - [存储层实现](#存储层实现)

**处理器**: `handleBase`

**实现文件**: `internal/handler/handler.go`- [实现权衡与限制](#实现权衡与限制)- [Blob 操作接口](#blob-操作接口)2. **`HEAD /v2/{name}/manifests/{reference}`**:

```go

func (h *Handler) handleBase(w http.ResponseWriter, r *http.Request) {

    w.Header().Set("Docker-Distribution-Api-Version", "registry/2.0")---- [实现权衡与限制](#实现权衡与限制)

    w.WriteHeader(http.StatusOK)

}

```

## 基础 API  - 实现了与 `GET` 相同的逻辑，但不返回响应体，用于高效地检查 Manifest 是否存在

**响应**:

- **200 OK**: 服务正常，支持 Docker Registry API v2
- **Headers**:

  - `Docker-Distribution-Api-Version: registry/2.0`### `GET /v2/`---3. **`PUT /v2/{name}/manifests/{reference}`**:

**设计说明**: 这是 Docker 客户端第一个调用的接口，用于确认服务器支持 v2 API。实现简单但必不可少。

**功能**: API 版本检查和兼容性验证  ## 基础 API   * 实现了上传 `Manifest` 和 `Manifest List`

## Manifest 操作接口**处理器**: `handleBase`

Manifest 是容器镜像的元数据描述，包含了镜像的配置信息和层信息。我们的实现完全支持 Docker Registry API v2 规范。**实现文件**: `internal/handler/handler.go`- 包含了至关重要的**服务端校验**：

### `GET /v2/{name}/manifests/{reference}`

**功能**: 获取指定仓库的 Manifest 内容  ```go### `GET /v2/`     * 检查 `Content-Type` 是否合法

**处理器**: `handleManifestGet`

**实现文件**: `internal/handler/manifests.go`func (h *Handler) handleBase(w http.ResponseWriter, r*http.Request) {

**参数**:    w.Header().Set("Docker-Distribution-Api-Version", "registry/2.0")     * 检查 `Manifest` 依赖的所有 `layers` 和 `config` blobs 是否都已存在。

- `name`: 仓库名称（如 `myapp`, `namespace/myapp`）
- `reference`: 标签名或 digest（如 `latest`, `v1.0`, `sha256:abc123...`）    w.WriteHeader(http.StatusOK)

**支持的 Content-Type**:}**功能**: API 版本检查和兼容性验证       * 检查 `digest` 的完整性。

- `application/vnd.docker.distribution.manifest.v2+json`
- `application/vnd.docker.distribution.manifest.list.v2+json````
- `application/vnd.oci.image.manifest.v1+json`
- `application/vnd.oci.image.index.v1+json`**处理器**: `handleBase`     * 返回了 `201 Created` 和 `Location` 头，符合 RESTful 规范。

**响应**:**响应**:

- **200 OK**: 成功返回 Manifest 内容

  - Headers:- **200 OK**: 服务正常，支持 Docker Registry API v2**实现文件**: `internal/handler/handler.go`4. **`DELETE /v2/{name}/manifests/{reference}`**:

    - `Content-Type`: Manifest 的媒体类型
    - `Content-Length`: 内容长度- **Headers**:
    - `Docker-Content-Digest`: SHA256 digest
- **404 Not Found**: Manifest 不存在  - `Docker-Distribution-Api-Version: registry/2.0````go   * 实现了一个**简化的、实用的**版本：通过`tag` 来“取消标记”一个 Manifest。

**实现特点**:

- 支持通过 `tag` 或 `digest` 查询
- 自动计算并返回 `Docker-Content-Digest`**设计说明**: 这是 Docker 客户端第一个调用的接口，用于确认服务器支持 v2 API。实现简单但必不可少。func (h *Handler) handleBase(w http.ResponseWriter, r*http.Request) {   * 明确了这是一个简化，并对不符合规范的 `DELETE` by `digest` 请求返回了 `405 Method Not Allowed`。
- 完整的错误处理和标准响应格式

### `HEAD /v2/{name}/manifests/{reference}`

---

w.Header().Set("Docker-Distribution-Api-Version", "registry/2.0")

**功能**: 检查 Manifest 是否存在（不返回内容）

**处理器**: `handleManifestHead`

**实现文件**: `internal/handler/manifests.go`

## Manifest 操作接口    w.WriteHeader(http.StatusOK)## Manifest PUT

**设计说明**: 与 GET 方法逻辑相同，但只返回 HTTP 头信息，用于高效检查资源存在性。

### `PUT /v2/{name}/manifests/{reference}`

Manifest 是容器镜像的元数据描述，包含了镜像的配置信息和层信息。我们的实现完全支持 Docker Registry API v2 规范。}

**功能**: 上传并存储 Manifest

**处理器**: `handleManifestPut`

**实现文件**: `internal/handler/manifests.go`

### `GET /v2/{name}/manifests/handleManifestPut`

**关键实现特性**:

1. **严格的 Content-Type 验证**:

   ```go**功能**:

   contentType := r.Header.Get("Content-Type")

   if !isValidManifestContentType(contentType) {**处理器**: `handleManifestGet`  

       registry.WriteErrorResponse(w, http.StatusBadRequest, 

           "MANIFEST_INVALID", "unsupported manifest media type")**实现文件**: `internal/handler/manifests.go`**响应**:* **前置检查** **: 严格检查** **Content-Type**，这是 API 规范的要求。

       return

   }

   ```

**参数**:- **200 OK**: 服务正常，支持 Docker Registry API v2
 **依赖校验**: 这是最关键的一步。我们解析 manifest，提取出它引用的所有 **config** **和** **layer** **的 digest，然后**逐一**调用** **storage.StatBlob** **确认它们都已存在。如果任何一个不存在，就拒绝此次** **PUT** **操作，并明确告诉客户端哪个 blob 缺失了。这完美地实现了文档中“此终结点通常在将所有层和配置 blob 上传到注册表后使用”的要求。**

2. **依赖关系校验**（核心特性）:

- `name`: 仓库名称（如 `myapp`, `namespace/myapp`）

  ```go

  // 解析 manifest，提取所有引用的 blob digest- `reference`: 标签名或 digest（如 `latest`, `v1.0`, `sha256:abc123...`）- **Headers**: * **完整性校验**: 我们计算了接收到的 manifest body 的 digest，并与 URL 中的 **reference** **(如果它也是一个 digest) 进行比较，确保数据没有损坏。**

  manifest := &registry.Manifest{}

  json.Unmarshal(body, manifest)



  // 验证 config blob 是否存在**支持的 Content-Type**:  - `Docker-Distribution-Api-Version: registry/2.0`* **持久化**: 所有检查通过后，才调用 **storage.PutManifest** **将其写入文件系统。**

  if _, err := h.Storage.StatBlob(manifest.Config.Digest); err != nil {

      registry.WriteErrorResponse(w, http.StatusBadRequest, - `application/vnd.docker.distribution.manifest.v2+json`

          "BLOB_UNKNOWN", fmt.Sprintf("config blob %s not found", 

          manifest.Config.Digest))- `application/vnd.docker.distribution.manifest.list.v2+json`* **成功响应**: 返回 **201 Created**，并提供 **Location** **和** **Docker-Content-Digest** **头，这些都是 RESTful API 设计的最佳实践。**

      return

  }- `application/vnd.oci.image.manifest.v1+json`



  // 验证所有 layer blob 是否存在- `application/vnd.oci.image.index.v1+json`**设计说明**: 这是 Docker 客户端第一个调用的接口，用于确认服务器支持 v2 API。实现简单但必不可少。

  for _, layer := range manifest.Layers {

      if _, err := h.Storage.StatBlob(layer.Digest); err != nil {

          registry.WriteErrorResponse(w, http.StatusBadRequest, 

              "BLOB_UNKNOWN", fmt.Sprintf("layer blob %s not found", **响应**:## Manifest DELETE

              layer.Digest))

          return- **200 OK**: 成功返回 Manifest 内容

      }

  }  - Headers:---

  ```

  - `Content-Type`: Manifest 的媒体类型

3. **完整性校验**:

   - 计算上传内容的 SHA256 digest    - `Content-Length`: 内容长度* **我们做了一个务实的简化，专注于最常见的用例：删除一个** **tag**。这对应于 **docker rmi my-image:tag** **后再** **push** **的场景。**
   - 与 URL 中的 reference（如果是 digest）进行比较
   - 确保数据传输过程中没有损坏    - `Docker-Content-Digest`: SHA256 digest

**响应**:- **404 Not Found**: Manifest 不存在## Manifest 操作接口* **我们明确地拒绝了通过** **digest** **删除 manifest 的请求，因为这通常应该由一个离线的、更复杂的垃圾回收程序来处理，以避免意外删除被其他** **tag** **引用的共享层。**

- **201 Created**: 成功上传并存储

  - Headers:

    - `Location`: Manifest 的访问 URL**实现特点**:* **成功的响应是** **202 Accepted**，表示请求已被接受处理，这完全符合文档规范。
    - `Docker-Content-Digest`: 计算得到的 digest
- **400 Bad Request**: Content-Type 不支持或依赖的 blob 不存在- 支持通过 `tag` 或 `digest` 查询
- **409 Conflict**: Digest 不匹配
- 自动计算并返回 `Docker-Content-Digest`Manifest 是容器镜像的元数据描述，包含了镜像的配置信息和层信息。我们的实现完全支持 Docker Registry API v2 规范。

### `DELETE /v2/{name}/manifests/{reference}`

- 完整的错误处理和标准响应格式

**功能**: 删除指定的 Manifest

**处理器**: `handleManifestDelete`  ## 需要注意的地方 (我们的简化与权衡)

**实现文件**: `internal/handler/manifests.go`

### `HEAD /v2/{name}/manifests/{reference}`

**实现策略**（简化版本）:

### `GET /v2/{name}/manifests/{reference}`

- ✅ **支持按标签删除**: `DELETE /v2/myapp/manifests/v1.0`
- ❌ **不支持按 digest 删除**: 避免复杂的引用计数和垃圾回收**功能**: 检查 Manifest 是否存在（不返回内容）

**设计理由**:**处理器**: `handleManifestHead`  在进入 Blob 部分之前，我们应该清楚地记下在 Manifest 部分做出的简化，这对于理解我们系统的行为至关重要：

- 按标签删除覆盖了最常见的使用场景**实现文件**: `internal/handler/manifests.go`
- 避免了实现复杂的引用计数机制
- 减少了并发删除时的数据一致性问题**功能**: 获取指定仓库的 Manifest 内容

**响应**:**设计说明**: 与 GET 方法逻辑相同，但只返回 HTTP 头信息，用于高效检查资源存在性。

- **202 Accepted**: 删除请求已接受处理**处理器**: `handleManifestGet`  1. **`DELETE` 行为**: 这是最大的简化。我们实现的是 `DELETE` by `tag`，而非规范中复杂的 `DELETE` by `digest`。这避免了实现引用计数和并发锁的巨大复杂性。对于本项目来说，这是一个非常合理的权衡。
- **405 Method Not Allowed**: 对按 digest 删除的请求
- **404 Not Found**: Manifest 不存在### `PUT /v2/{name}/manifests/{reference}`

---**实现文件**: `internal/handler/manifests.go`2. **`Content-Type` 的持久化**: 我们的 `GetManifest` 目前是硬编码返回 `...manifest.v2+json`。一个完整的实现需要在 `PutManifest` 时将客户端提供的 `Content-Type`**存储**起来，然后在 `GetManifest` 时**读取**并返回。这会影响客户端能否正确区分 `Manifest` 和 `Manifest List`。对于目前的 `push`/`pull` 流程，这暂时不构成阻碍，但这是一个重要的待办事项。

## Blob 操作接口**功能**: 上传并存储 Manifest

Blob 是容器镜像的实际数据内容，包括配置文件和文件系统层。我们实现了完整的分片上传流程。**处理器**: `handleManifestPut`  3. **`Accept` 头的校验**: 我们的 `GetManifest` 没有检查客户端请求中的 `Accept` 头。一个更严格的服务器会确保它将要发送的 `Content-Type` 是客户端声明可以接受的类型之一。

### Blob 查询接口**实现文件**: `internal/handler/manifests.go`

#### `HEAD /v2/{name}/blobs/{digest}`**参数**:4. **授权 (Authorization)**: 根据项目要求，我们完全跳过了这一部分。在生产环境中，每个对 Manifest 的 `GET`, `PUT`, `DELETE` 操作都需要严格的认证和授权检查（用户是否有权限读/写/删除这个仓库）

**功能**: 检查指定 Blob 是否存在  **关键实现特性**:

**处理器**: `handleBlobHead`

**实现文件**: `internal/handler/blobs.go`- `name`: 仓库名称（如 `myapp`, `namespace/myapp`）

**响应**:1. **严格的 Content-Type 验证**:- `reference`: 标签名或 digest（如 `latest`, `v1.0`, `sha256:abc123...`）

- **200 OK**: Blob 存在   ```go

  - Headers: `Content-Length`, `Docker-Content-Digest`
- **404 Not Found**: Blob 不存在   contentType := r.Header.Get("Content-Type")**支持的 Content-Type**:

#### `GET /v2/{name}/blobs/{digest}`   if !isValidManifestContentType(contentType) {- `application/vnd.docker.distribution.manifest.v2+json`

**功能**: 下载指定的 Blob 内容         registry.WriteErrorResponse(w, http.StatusBadRequest, - `application/vnd.docker.distribution.manifest.list.v2+json`

**处理器**: `handleBlobGet`

**实现文件**: `internal/handler/blobs.go`           "MANIFEST_INVALID", "unsupported manifest media type")- `application/vnd.oci.image.manifest.v1+json`

**实现特点**:       return- `application/vnd.oci.image.index.v1+json`

```go

// 获取 blob 内容流

blobReader, err := h.Storage.GetBlob(digest)   ```**响应**:

defer blobReader.Close()

- **200 OK**: 成功返回 Manifest 内容

// 获取大小信息用于设置 Content-Length

size, err := h.Storage.StatBlob(digest)2. **依赖关系校验**（核心特性）:  - Headers:



// 流式传输，避免大文件内存占用    - `Content-Type`: Manifest 的媒体类型

io.Copy(w, blobReader)

```   ```go    - `Content-Length`: 内容长度



### Blob 上传接口（分片上传流程）   // 解析 manifest，提取所有引用的 blob digest    - `Docker-Content-Digest`: SHA256 digest



Docker Registry 支持分片上传，允许客户端将大文件分成多个块上传，支持断点续传。   manifest := &registry.Manifest{}- **404 Not Found**: Manifest 不存在



#### 1. `POST /v2/{name}/blobs/uploads/`   json.Unmarshal(body, manifest)



**功能**: 开始新的 Blob 上传会话     **实现特点**:

**处理器**: `handleBlobUploadStart`  

**实现文件**: `internal/handler/blobs.go`   // 验证 config blob 是否存在- 支持通过 `tag` 或 `digest` 查询



**实现流程**:   if _, err := h.Storage.StatBlob(manifest.Config.Digest); err != nil {- 自动计算并返回 `Docker-Content-Digest`



```go       registry.WriteErrorResponse(w, http.StatusBadRequest, - 完整的错误处理和标准响应格式

// 生成唯一的上传 UUID

uploadUUID := uuid.New().String()           "BLOB_UNKNOWN", fmt.Sprintf("config blob %s not found", 



// 创建上传会话           manifest.Config.Digest))### `HEAD /v2/{name}/manifests/{reference}`

err := h.Storage.StartUpload(repoName, uploadUUID)

       return

// 返回上传 URL

location := fmt.Sprintf("/v2/%s/blobs/uploads/%s", repoName, uploadUUID)   }**功能**: 检查 Manifest 是否存在（不返回内容）  

w.Header().Set("Location", location)

w.Header().Set("Docker-Upload-UUID", uploadUUID)   **处理器**: `handleManifestHead`  

```

   // 验证所有 layer blob 是否存在**实现文件**: `internal/handler/manifests.go`

**响应**:

   for _, layer := range manifest.Layers {

- **202 Accepted**: 上传会话创建成功
- Headers:       if _, err := h.Storage.StatBlob(layer.Digest); err != nil {**设计说明**: 与 GET 方法逻辑相同，但只返回 HTTP 头信息，用于高效检查资源存在性。

  - `Location`: 上传会话的 URL
  - `Docker-Upload-UUID`: 上传会话 ID           registry.WriteErrorResponse(w, http.StatusBadRequest,
  - `Range`: `0-0`（初始范围）

    "BLOB_UNKNOWN", fmt.Sprintf("layer blob %s not found", ### `PUT /v2/{name}/manifests/{reference}`

#### 2. `GET /v2/{name}/blobs/uploads/{uuid}`

    layer.Digest))

**功能**: 查询上传会话状态

**处理器**: `handleBlobUploadStatus`           return**功能**: 上传并存储 Manifest

**响应**:       }**处理器**: `handleManifestPut`

- **204 No Content**: 上传会话存在   }**实现文件**: `internal/handler/manifests.go`
- Headers:

  - `Range`: 当前已上传的字节范围   ```
  - `Docker-Upload-UUID`: 上传会话 ID

**关键实现特性**:

#### 3. `PATCH /v2/{name}/blobs/uploads/{uuid}`

3. **完整性校验**:

**功能**: 上传数据分片

**处理器**: `handleBlobUploadChunk`     - 计算上传内容的 SHA256 digest1. **严格的 Content-Type 验证**:

**实现文件**: `internal/handler/blobs.go`

- 与 URL 中的 reference（如果是 digest）进行比较   ```go

**实现特点**:

- 确保数据传输过程中没有损坏   contentType := r.Header.Get("Content-Type")

```go

// 流式读取请求体，避免内存占用   if !isValidManifestContentType(contentType) {

newSize, err := h.Storage.AppendChunk(repoName, uploadUUID, r.Body)

**响应**:       registry.WriteErrorResponse(w, http.StatusBadRequest, 

// 更新 Range 头

w.Header().Set("Range", fmt.Sprintf("0-%d", newSize-1))           "MANIFEST_INVALID", "unsupported manifest media type")

w.Header().Set("Docker-Upload-UUID", uploadUUID)

```- **201 Created**: 成功上传并存储       return



**支持的上传方式**:  - Headers:   }



- 顺序分片上传    - `Location`: Manifest 的访问 URL   ```

- 单次完整上传

- 断点续传    - `Docker-Content-Digest`: 计算得到的 digest



#### 4. `PUT /v2/{name}/blobs/uploads/{uuid}?digest={digest}`- **400 Bad Request**: Content-Type 不支持或依赖的 blob 不存在2. **依赖关系校验**（核心特性）:



**功能**: 完成上传并进行完整性校验  - **409 Conflict**: Digest 不匹配

**处理器**: `handleBlobUploadCommit`  

**实现文件**: `internal/handler/blobs.go`   ```go



**核心实现**:### `DELETE /v2/{name}/manifests/{reference}`   // 解析 manifest，提取所有引用的 blob digest



```go   manifest := &registry.Manifest{}

// 1. 如果 PUT 请求包含数据，先追加到文件

if r.ContentLength > 0 {**功能**: 删除指定的 Manifest     json.Unmarshal(body, manifest)

    h.Storage.AppendChunk(repoName, uploadID, r.Body)

}**处理器**: `handleManifestDelete`   



// 2. 进行完整性校验并提交**实现文件**: `internal/handler/manifests.go`   // 验证 config blob 是否存在

err = h.Storage.CommitUpload(repoName, uploadID, digest)

```   if _, err := h.Storage.StatBlob(manifest.Config.Digest); err != nil {



**CommitUpload 的关键逻辑**:**实现策略**（简化版本）:       registry.WriteErrorResponse(w, http.StatusBadRequest, 



```go           "BLOB_UNKNOWN", fmt.Sprintf("config blob %s not found", 

// 计算文件的实际 SHA256 digest

file, err := os.Open(dataPath)- ✅ **支持按标签删除**: `DELETE /v2/myapp/manifests/v1.0`           manifest.Config.Digest))

hasher := sha256.New()

io.Copy(hasher, file)- ❌ **不支持按 digest 删除**: 避免复杂的引用计数和垃圾回收       return

file.Close() // 重要：立即关闭文件句柄

   }

calculatedDigest := "sha256:" + hex.EncodeToString(hasher.Sum(nil))

**设计理由**:   

// 校验 digest 是否匹配

if calculatedDigest != expectedDigest {   // 验证所有 layer blob 是否存在

    return registry.ErrDigestMismatch

}- 按标签删除覆盖了最常见的使用场景   for _, layer := range manifest.Layers {



// 将临时文件移动到最终位置（原子操作）- 避免了实现复杂的引用计数机制       if _, err := h.Storage.StatBlob(layer.Digest); err != nil {

os.Rename(dataPath, finalPath)

```- 减少了并发删除时的数据一致性问题           registry.WriteErrorResponse(w, http.StatusBadRequest, 



**响应**:               "BLOB_UNKNOWN", fmt.Sprintf("layer blob %s not found", 



- **201 Created**: 上传成功**响应**:               layer.Digest))

- Headers:

  - `Location`: Blob 的访问 URL           return

  - `Docker-Content-Digest`: 验证后的 digest

- **400 Bad Request**: Digest 不匹配- **202 Accepted**: 删除请求已接受处理       }



#### 5. `DELETE /v2/{name}/blobs/uploads/{uuid}`- **405 Method Not Allowed**: 对按 digest 删除的请求   }



**功能**: 取消上传会话  - **404 Not Found**: Manifest 不存在   ```

**处理器**: `handleBlobUploadCancel`



**实现**: 删除临时上传目录和所有相关文件

---3. **完整性校验**:

---

   - 计算上传内容的 SHA256 digest

## 存储层实现

## Blob 操作接口   - 与 URL 中的 reference（如果是 digest）进行比较

### 文件系统布局

   - 确保数据传输过程中没有损坏

```

{REGISTRY_STORAGE_ROOT}/Blob 是容器镜像的实际数据内容，包括配置文件和文件系统层。我们实现了完整的分片上传流程。

└── docker/

    └── registry/**响应**:

    └── v2/

    ├── blobs/                    # 内容寻址存储### Blob 查询接口

    │   └── sha256/

    │       └── {digest[0:2]}/    # 按 digest 前两位分目录-**201 Created**: 成功上传并存储

    │           └── {digest}      # 实际 blob 文件

    └── repositories/             # 仓库元数据####`HEAD /v2/{name}/blobs/{digest}`  - Headers:

    └── {repository}/

    ├── _manifests/    -`Location`: Manifest 的访问 URL

    │   ├── revisions/    # 按 digest 存储的 manifest

    │   └── tags/         # 标签到 digest 的映射**功能**: 检查指定 Blob 是否存在      - `Docker-Content-Digest`: 计算得到的 digest

    └── _uploads/         # 临时上传会话

    └── {uuid}/**处理器**: `handleBlobHead`  - **400 Bad Request**: Content-Type 不支持或依赖的 blob 不存在

    ├── data      # 上传的数据

    └── startedat # 上传开始时间**实现文件**: `internal/handler/blobs.go`- **409 Conflict**: Digest 不匹配

```



### 关键存储操作

**响应**:### `DELETE /v2/{name}/manifests/{reference}`

#### Blob 存储



```go

// getBlobPath 生成基于 digest 的存储路径- **200 OK**: Blob 存在**功能**: 删除指定的 Manifest  

func (fs *FileSystemStorage) getBlobPath(digest string) (string, error) {

    if !strings.HasPrefix(digest, "sha256:") {  - Headers: `Content-Length`, `Docker-Content-Digest`**处理器**: `handleManifestDelete`  

        return "", fmt.Errorf("unsupported digest format")

    }- **404 Not Found**: Blob 不存在**实现文件**: `internal/handler/manifests.go`

  

    hash := digest[7:] // 移除 "sha256:" 前缀

    // 按前两位字符分目录，提高文件系统性能

    return filepath.Join(fs.blobsPath(), "sha256", hash[0:2], hash), nil#### `GET /v2/{name}/blobs/{digest}`**实现策略**（简化版本）:

}

```

#### Manifest 存储**功能**: 下载指定的 Blob 内容  - ✅ **支持按标签删除**: `DELETE /v2/myapp/manifests/v1.0`

```go**处理器**: `handleBlobGet`  - ❌ **不支持按 digest 删除**: 避免复杂的引用计数和垃圾回收

// PutManifest 存储 manifest 并创建标签映射

func (fs *FileSystemStorage) PutManifest(repoName, reference, contentType string, manifest []byte) (string, error) {**实现文件**: `internal/handler/blobs.go`

    // 1. 计算 digest

    hasher := sha256.New()**设计理由**:

    hasher.Write(manifest)

    digest := "sha256:" + hex.EncodeToString(hasher.Sum(nil))**实现特点**:

    // 2. 存储 manifest 内容到 revisions- 按标签删除覆盖了最常见的使用场景

    revisionPath := filepath.Join(fs.getManifestRevisionsPath(repoName), "sha256", digest[7:])

    ```go- 避免了实现复杂的引用计数机制

    // 3. 如果 reference 是标签，创建标签映射

    if !strings.HasPrefix(reference, "sha256:") {// 获取 blob 内容流- 减少了并发删除时的数据一致性问题

    tagPath := filepath.Join(fs.getManifestTagsPath(repoName), reference)

    // 存储标签到 digest 的映射blobReader, err := h.Storage.GetBlob(digest)

    }

    defer blobReader.Close()**响应**:

    return digest, nil

}

```

// 获取大小信息用于设置 Content-Length- **202 Accepted**: 删除请求已接受处理

---

size, err := h.Storage.StatBlob(digest)- **405 Method Not Allowed**: 对按 digest 删除的请求

## 实现权衡与限制

- **404 Not Found**: Manifest 不存在

### 实现策略说明

// 流式传输，避免大文件内存占用

在设计本项目时，我们做出了一些重要的权衡决策，这些决策在保证核心功能的同时简化了实现复杂度。

io.Copy(w, blobReader)---

### ✅ 完整实现的特性

```

1. **完整的 Docker Registry API v2 核心功能**

   - 所有必需的端点都已实现## Blob 操作接口
   - 符合官方 API 规范

### Blob 上传接口（分片上传流程）

2. **数据完整性保证**

   - SHA256 校验确保数据完整性Blob 是容器镜像的实际数据内容，包括配置文件和文件系统层。我们实现了完整的分片上传流程。
   - 原子性操作避免部分失败状态
   - 文件句柄正确管理，避免锁定问题Docker Registry 支持分片上传，允许客户端将大文件分成多个块上传，支持断点续传。
3. **高性能设计**### Blob 查询接口

   - 流式处理，支持大文件
   - 内容寻址存储，自动去重#### 1. `POST /v2/{name}/blobs/uploads/`
   - 分片上传支持断点续传

#### `HEAD /v2/{name}/blobs/{digest}`

4. **跨平台兼容性**

   - Windows 和 Linux 路径自动适配**功能**: 开始新的 Blob 上传会话
   - 文件操作跨平台兼容

**处理器**: `handleBlobUploadStart`  **功能**: 检查指定 Blob 是否存在

### ⚠️ 简化的实现

**实现文件**: `internal/handler/blobs.go`**处理器**: `handleBlobHead`

1. **DELETE 操作限制**

**实现文件**: `internal/handler/blobs.go`

```

   ✅ 支持: DELETE /v2/{name}/manifests/{tag}**实现流程**:

   ❌ 不支持: DELETE /v2/{name}/manifests/{digest}

   ```**响应**:



   **理由**: 避免实现复杂的引用计数和垃圾回收机制```go



2. **Content-Type 持久化**// 生成唯一的上传 UUID- **200 OK**: Blob 存在

   - 当前 `GetManifest` 硬编码返回 `application/vnd.docker.distribution.manifest.v2+json`

   - 完整实现需要存储并返回客户端原始的 Content-TypeuploadUUID := uuid.New().String()  - Headers: `Content-Length`, `Docker-Content-Digest`



3. **Accept 头验证**- **404 Not Found**: Blob 不存在

   - 未检查客户端的 `Accept` 头

   - 生产环境需要验证客户端支持的媒体类型// 创建上传会话



4. **认证授权**err := h.Storage.StartUpload(repoName, uploadUUID)#### `GET /v2/{name}/blobs/{digest}`

   - 完全跳过用户认证和权限控制

   - 适用于内网环境，生产环境需要安全机制



### 🎯 设计选择的合理性// 返回上传 URL**功能**: 下载指定的 Blob 内容  



1. **简化 vs 复杂性**: 在保证核心功能的前提下，避免过度工程化location := fmt.Sprintf("/v2/%s/blobs/uploads/%s", repoName, uploadUUID)**处理器**: `handleBlobGet`  

2. **性能 vs 功能**: 优先保证基本功能正确性，然后考虑性能优化

3. **兼容性 vs 实现成本**: 在关键接口保持兼容，非关键特性可以简化w.Header().Set("Location", location)**实现文件**: `internal/handler/blobs.go`

4. **安全性 vs 易用性**: 开发测试环境优先易用性，生产环境需要额外安全措施

w.Header().Set("Docker-Upload-UUID", uploadUUID)

这种权衡使得项目在保持核心功能完整的同时，代码结构清晰，易于理解和维护。

```**实现特点**:

### 具体的简化说明



#### Manifest PUT 实现的核心价值

**响应**:```go

我们的 `handleManifestPut` 实现包含了至关重要的服务端校验：

// 获取 blob 内容流

- **前置检查**: 严格检查 Content-Type，这是 API 规范的要求

- **依赖校验**: 解析 manifest，提取所有引用的 config 和 layer 的 digest，逐一确认它们都已存在- **202 Accepted**: 上传会话创建成功blobReader, err := h.Storage.GetBlob(digest)

- **完整性校验**: 计算接收到的 manifest body 的 digest，与 URL 中的 reference 进行比较

- **持久化**: 所有检查通过后，才调用 storage.PutManifest 将其写入文件系统- Headers:defer blobReader.Close()

- **成功响应**: 返回 201 Created，并提供 Location 和 Docker-Content-Digest 头

  - `Location`: 上传会话的 URL

#### Manifest DELETE 实现策略

  - `Docker-Upload-UUID`: 上传会话 ID// 获取大小信息用于设置 Content-Length

- 专注于最常见的用例：删除一个 tag

- 明确拒绝通过 digest 删除 manifest 的请求，避免复杂的垃圾回收逻辑  - `Range`: `0-0`（初始范围）size, err := h.Storage.StatBlob(digest)

- 成功响应是 202 Accepted，表示请求已被接受处理



#### 其他简化说明

#### 2. `GET /v2/{name}/blobs/uploads/{uuid}`// 流式传输，避免大文件内存占用

1. **Content-Type 的持久化**: 当前硬编码返回特定类型，完整实现需要存储客户端原始的 Content-Type

io.Copy(w, blobReader)

2. **Accept 头的校验**: 未检查客户端请求中的 Accept 头，生产环境需要更严格的验证

**功能**: 查询上传会话状态  ```

3. **授权机制**: 完全跳过认证和权限控制，适用于内网开发测试环境
**处理器**: `handleBlobUploadStatus`

### Blob 上传接口（分片上传流程）

**响应**:

Docker Registry 支持分片上传，允许客户端将大文件分成多个块上传，支持断点续传。

- **204 No Content**: 上传会话存在

- Headers:#### 1. `POST /v2/{name}/blobs/uploads/`

  - `Range`: 当前已上传的字节范围

  - `Docker-Upload-UUID`: 上传会话 ID**功能**: 开始新的 Blob 上传会话  

**处理器**: `handleBlobUploadStart`  

#### 3. `PATCH /v2/{name}/blobs/uploads/{uuid}`**实现文件**: `internal/handler/blobs.go`



**功能**: 上传数据分片  **实现流程**:

**处理器**: `handleBlobUploadChunk`  

**实现文件**: `internal/handler/blobs.go````go

// 生成唯一的上传 UUID

**实现特点**:uploadUUID := uuid.New().String()



```go// 创建上传会话

// 流式读取请求体，避免内存占用err := h.Storage.StartUpload(repoName, uploadUUID)

newSize, err := h.Storage.AppendChunk(repoName, uploadUUID, r.Body)

// 返回上传 URL

// 更新 Range 头location := fmt.Sprintf("/v2/%s/blobs/uploads/%s", repoName, uploadUUID)

w.Header().Set("Range", fmt.Sprintf("0-%d", newSize-1))w.Header().Set("Location", location)

w.Header().Set("Docker-Upload-UUID", uploadUUID)w.Header().Set("Docker-Upload-UUID", uploadUUID)

```

**支持的上传方式**:**响应**:

- 顺序分片上传- **202 Accepted**: 上传会话创建成功
- 单次完整上传- Headers:
- 断点续传  - `Location`: 上传会话的 URL

  - `Docker-Upload-UUID`: 上传会话 ID

#### 4. `PUT /v2/{name}/blobs/uploads/{uuid}?digest={digest}`  - `Range`: `0-0`（初始范围）

**功能**: 完成上传并进行完整性校验  #### 2. `GET /v2/{name}/blobs/uploads/{uuid}`

**处理器**: `handleBlobUploadCommit`

**实现文件**: `internal/handler/blobs.go`**功能**: 查询上传会话状态

**处理器**: `handleBlobUploadStatus`

**核心实现**:

**响应**:

```go

// 1. 如果 PUT 请求包含数据，先追加到文件- **204 No Content**: 上传会话存在

if r.ContentLength > 0 {- Headers:

    h.Storage.AppendChunk(repoName, uploadID, r.Body)  - `Range`: 当前已上传的字节范围

}  - `Docker-Upload-UUID`: 上传会话 ID



// 2. 进行完整性校验并提交#### 3. `PATCH /v2/{name}/blobs/uploads/{uuid}`

err = h.Storage.CommitUpload(repoName, uploadID, digest)

```**功能**: 上传数据分片  

**处理器**: `handleBlobUploadChunk`  

**CommitUpload 的关键逻辑**:**实现文件**: `internal/handler/blobs.go`



```go**实现特点**:

// 计算文件的实际 SHA256 digest

file, err := os.Open(dataPath)```go

hasher := sha256.New()// 流式读取请求体，避免内存占用

io.Copy(hasher, file)newSize, err := h.Storage.AppendChunk(repoName, uploadUUID, r.Body)

file.Close() // 重要：立即关闭文件句柄

// 更新 Range 头

calculatedDigest := "sha256:" + hex.EncodeToString(hasher.Sum(nil))w.Header().Set("Range", fmt.Sprintf("0-%d", newSize-1))

w.Header().Set("Docker-Upload-UUID", uploadUUID)

// 校验 digest 是否匹配```

if calculatedDigest != expectedDigest {

    return registry.ErrDigestMismatch**支持的上传方式**:

}

- 顺序分片上传

// 将临时文件移动到最终位置（原子操作）- 单次完整上传

os.Rename(dataPath, finalPath)- 断点续传

```

#### 4. `PUT /v2/{name}/blobs/uploads/{uuid}?digest={digest}`

**响应**:

**功能**: 完成上传并进行完整性校验

- **201 Created**: 上传成功**处理器**: `handleBlobUploadCommit`
- Headers:**实现文件**: `internal/handler/blobs.go`

  - `Location`: Blob 的访问 URL
  - `Docker-Content-Digest`: 验证后的 digest**核心实现**:
- **400 Bad Request**: Digest 不匹配

```go

#### 5. `DELETE /v2/{name}/blobs/uploads/{uuid}`// 1. 如果 PUT 请求包含数据，先追加到文件

if r.ContentLength > 0 {

**功能**: 取消上传会话      h.Storage.AppendChunk(repoName, uploadID, r.Body)

**处理器**: `handleBlobUploadCancel`}



**实现**: 删除临时上传目录和所有相关文件// 2. 进行完整性校验并提交

err = h.Storage.CommitUpload(repoName, uploadID, digest)

---```



## 存储层实现**CommitUpload 的关键逻辑**:



### 文件系统布局```go

// 计算文件的实际 SHA256 digest

```file, err := os.Open(dataPath)

{REGISTRY_STORAGE_ROOT}/hasher := sha256.New()

└── docker/io.Copy(hasher, file)

    └── registry/file.Close() // 重要：立即关闭文件句柄

        └── v2/

            ├── blobs/                    # 内容寻址存储calculatedDigest := "sha256:" + hex.EncodeToString(hasher.Sum(nil))

            │   └── sha256/

            │       └── {digest[0:2]}/    # 按 digest 前两位分目录// 校验 digest 是否匹配

            │           └── {digest}      # 实际 blob 文件if calculatedDigest != expectedDigest {

            └── repositories/             # 仓库元数据    return registry.ErrDigestMismatch

                └── {repository}/}

                    ├── _manifests/

                    │   ├── revisions/    # 按 digest 存储的 manifest// 将临时文件移动到最终位置（原子操作）

                    │   └── tags/         # 标签到 digest 的映射os.Rename(dataPath, finalPath)

                    └── _uploads/         # 临时上传会话```

                        └── {uuid}/

                            ├── data      # 上传的数据**响应**:

                            └── startedat # 上传开始时间

```- **201 Created**: 上传成功

- Headers:

### 关键存储操作  - `Location`: Blob 的访问 URL

  - `Docker-Content-Digest`: 验证后的 digest

#### Blob 存储- **400 Bad Request**: Digest 不匹配



```go#### 5. `DELETE /v2/{name}/blobs/uploads/{uuid}`

// getBlobPath 生成基于 digest 的存储路径

func (fs *FileSystemStorage) getBlobPath(digest string) (string, error) {**功能**: 取消上传会话  

    if !strings.HasPrefix(digest, "sha256:") {**处理器**: `handleBlobUploadCancel`

        return "", fmt.Errorf("unsupported digest format")

    }**实现**: 删除临时上传目录和所有相关文件

  

    hash := digest[7:] // 移除 "sha256:" 前缀---

    // 按前两位字符分目录，提高文件系统性能

    return filepath.Join(fs.blobsPath(), "sha256", hash[0:2], hash), nil## 存储层实现

}

```### 文件系统布局



#### Manifest 存储```

{REGISTRY_STORAGE_ROOT}/

```go└── docker/

// PutManifest 存储 manifest 并创建标签映射    └── registry/

func (fs *FileSystemStorage) PutManifest(repoName, reference, contentType string, manifest []byte) (string, error) {        └── v2/

    // 1. 计算 digest            ├── blobs/                    # 内容寻址存储

    hasher := sha256.New()            │   └── sha256/

    hasher.Write(manifest)            │       └── {digest[0:2]}/    # 按 digest 前两位分目录

    digest := "sha256:" + hex.EncodeToString(hasher.Sum(nil))            │           └── {digest}      # 实际 blob 文件

            └── repositories/             # 仓库元数据

    // 2. 存储 manifest 内容到 revisions                └── {repository}/

    revisionPath := filepath.Join(fs.getManifestRevisionsPath(repoName), "sha256", digest[7:])                    ├── _manifests/

                        │   ├── revisions/    # 按 digest 存储的 manifest

    // 3. 如果 reference 是标签，创建标签映射                    │   └── tags/         # 标签到 digest 的映射

    if !strings.HasPrefix(reference, "sha256:") {                    └── _uploads/         # 临时上传会话

        tagPath := filepath.Join(fs.getManifestTagsPath(repoName), reference)                        └── {uuid}/

        // 存储标签到 digest 的映射                            ├── data      # 上传的数据

    }                            └── startedat # 上传开始时间

    ```

    return digest, nil

}### 关键存储操作

```

#### Blob 存储

---

```go

## 实现权衡与限制// getBlobPath 生成基于 digest 的存储路径

func (fs *FileSystemStorage) getBlobPath(digest string) (string, error) {

### 实现策略说明    if !strings.HasPrefix(digest, "sha256:") {

        return "", fmt.Errorf("unsupported digest format")

在设计本项目时，我们做出了一些重要的权衡决策，这些决策在保证核心功能的同时简化了实现复杂度：    }

  

### ✅ 完整实现的特性    hash := digest[7:] // 移除 "sha256:" 前缀

    // 按前两位字符分目录，提高文件系统性能

1. **完整的 Docker Registry API v2 核心功能**    return filepath.Join(fs.blobsPath(), "sha256", hash[0:2], hash), nil

   - 所有必需的端点都已实现}

   - 符合官方 API 规范```



2. **数据完整性保证**#### Manifest 存储

   - SHA256 校验确保数据完整性

   - 原子性操作避免部分失败状态```go

   - 文件句柄正确管理，避免锁定问题// PutManifest 存储 manifest 并创建标签映射

func (fs *FileSystemStorage) PutManifest(repoName, reference, contentType string, manifest []byte) (string, error) {

3. **高性能设计**    // 1. 计算 digest

   - 流式处理，支持大文件    hasher := sha256.New()

   - 内容寻址存储，自动去重    hasher.Write(manifest)

   - 分片上传支持断点续传    digest := "sha256:" + hex.EncodeToString(hasher.Sum(nil))



4. **跨平台兼容性**    // 2. 存储 manifest 内容到 revisions

   - Windows 和 Linux 路径自动适配    revisionPath := filepath.Join(fs.getManifestRevisionsPath(repoName), "sha256", digest[7:])

   - 文件操作跨平台兼容  

    // 3. 如果 reference 是标签，创建标签映射

### ⚠️ 简化的实现    if !strings.HasPrefix(reference, "sha256:") {

        tagPath := filepath.Join(fs.getManifestTagsPath(repoName), reference)

1. **DELETE 操作限制**        // 存储标签到 digest 的映射

    }

```

   ✅ 支持: DELETE /v2/{name}/manifests/{tag}    return digest, nil

   ❌ 不支持: DELETE /v2/{name}/manifests/{digest}}

``````



   **理由**: 避免实现复杂的引用计数和垃圾回收机制---



2. **Content-Type 持久化**## 实现权衡与限制

   - 当前 `GetManifest` 硬编码返回 `application/vnd.docker.distribution.manifest.v2+json`

   - 完整实现需要存储并返回客户端原始的 Content-Type### ✅ 完整实现的特性



3. **Accept 头验证**1. **完整的 Docker Registry API v2 核心功能**

   - 未检查客户端的 `Accept` 头   - 所有必需的端点都已实现

   - 生产环境需要验证客户端支持的媒体类型   - 符合官方 API 规范



4. **认证授权**2. **数据完整性保证**

   - 完全跳过用户认证和权限控制   - SHA256 校验确保数据完整性

   - 适用于内网环境，生产环境需要安全机制   - 原子性操作避免部分失败状态

   - 文件句柄正确管理，避免锁定问题

### 🎯 设计选择的合理性

3. **高性能设计**

1. **简化 vs 复杂性**: 在保证核心功能的前提下，避免过度工程化   - 流式处理，支持大文件

2. **性能 vs 功能**: 优先保证基本功能正确性，然后考虑性能优化   - 内容寻址存储，自动去重

3. **兼容性 vs 实现成本**: 在关键接口保持兼容，非关键特性可以简化   - 分片上传支持断点续传

4. **安全性 vs 易用性**: 开发测试环境优先易用性，生产环境需要额外安全措施

4. **跨平台兼容性**

这种权衡使得项目在保持核心功能完整的同时，代码结构清晰，易于理解和维护。   - Windows 和 Linux 路径自动适配

   - 文件操作跨平台兼容

### 具体的简化说明

### ⚠️ 简化的实现

1. **Manifest DELETE 行为**: 这是最大的简化。我们实现的是 `DELETE` by `tag`，而非规范中复杂的 `DELETE` by `digest`。这避免了实现引用计数和并发锁的巨大复杂性。对于本项目来说，这是一个非常合理的权衡。

1. **DELETE 操作限制**

2. **Content-Type 的持久化**: 我们的 `GetManifest` 目前是硬编码返回 `...manifest.v2+json`。一个完整的实现需要在 `PutManifest` 时将客户端提供的 `Content-Type` 存储起来，然后在 `GetManifest` 时读取并返回。这会影响客户端能否正确区分 `Manifest` 和 `Manifest List`。对于目前的 `push`/`pull` 流程，这暂时不构成阻碍，但这是一个重要的待办事项。

   ```

3. **Accept 头的校验**: 我们的 `GetManifest` 没有检查客户端请求中的 `Accept` 头。一个更严格的服务器会确保它将要发送的 `Content-Type` 是客户端声明可以接受的类型之一。   ✅ 支持: DELETE /v2/{name}/manifests/{tag}

   ❌ 不支持: DELETE /v2/{name}/manifests/{digest}

4. **授权 (Authorization)**: 根据项目要求，我们完全跳过了这一部分。在生产环境中，每个对 Manifest 的 `GET`, `PUT`, `DELETE` 操作都需要严格的认证和授权检查（用户是否有权限读/写/删除这个仓库）。   ```



### Manifest PUT 实现的核心价值   **理由**: 避免实现复杂的引用计数和垃圾回收机制



我们的 `handleManifestPut` 实现包含了至关重要的服务端校验：2. **Content-Type 持久化**

   - 当前 `GetManifest` 硬编码返回 `application/vnd.docker.distribution.manifest.v2+json`

- **前置检查**: 严格检查 Content-Type，这是 API 规范的要求。   - 完整实现需要存储并返回客户端原始的 Content-Type

- **依赖校验**: 这是最关键的一步。我们解析 manifest，提取出它引用的所有 config 和 layer 的 digest，然后逐一调用 storage.StatBlob 确认它们都已存在。如果任何一个不存在，就拒绝此次 PUT 操作，并明确告诉客户端哪个 blob 缺失了。这完美地实现了文档中"此终结点通常在将所有层和配置 blob 上传到注册表后使用"的要求。

- **完整性校验**: 我们计算了接收到的 manifest body 的 digest，并与 URL 中的 reference（如果它也是一个 digest）进行比较，确保数据没有损坏。3. **Accept 头验证**

- **持久化**: 所有检查通过后，才调用 storage.PutManifest 将其写入文件系统。   - 未检查客户端的 `Accept` 头

- **成功响应**: 返回 201 Created，并提供 Location 和 Docker-Content-Digest 头，这些都是 RESTful API 设计的最佳实践。   - 生产环境需要验证客户端支持的媒体类型



### Manifest DELETE 实现策略4. **认证授权**

   - 完全跳过用户认证和权限控制

- 我们做了一个务实的简化，专注于最常见的用例：删除一个 tag。这对应于 `docker rmi my-image:tag` 后再 push 的场景。   - 适用于内网环境，生产环境需要安全机制

- 我们明确地拒绝了通过 digest 删除 manifest 的请求，因为这通常应该由一个离线的、更复杂的垃圾回收程序来处理，以避免意外删除被其他 tag 引用的共享层。

- 成功的响应是 202 Accepted，表示请求已被接受处理，这完全符合文档规范。### 🎯 设计选择的合理性

1. **简化 vs 复杂性**: 在保证核心功能的前提下，避免过度工程化
2. **性能 vs 功能**: 优先保证基本功能正确性，然后考虑性能优化
3. **兼容性 vs 实现成本**: 在关键接口保持兼容，非关键特性可以简化
4. **安全性 vs 易用性**: 开发测试环境优先易用性，生产环境需要额外安全措施

这种权衡使得项目在保持核心功能完整的同时，代码结构清晰，易于理解和维护。
``````
