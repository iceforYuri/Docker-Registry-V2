## 简化版 Docker Registry V2 后端服务 - 设计与实现文档

### 项目概述

本项目旨在实现一个符合 Docker Registry HTTP API V2 规范的简化后端服务。该服务使用 Go 语言编写，专注于支持 Docker 客户端核心的 `push` 和 `pull` 操作流程。项目在满足核心功能的前提下，对部分特性（如认证授权）进行了简化，以聚焦于 Registry 的核心工作原理和Go 语言后端实践

### 1. 需求分析与理解

首先对项目要求进行了深入分析，将其分解为以下几个核心要点：

* **核心功能**:

  1. **实现 V2 API 规范**
  2. **Blob 分片上传**: 必须正确处理从 `POST` (开始)、`PATCH` (上传块) 到 `PUT` (完成) 的完整分片上传流程，支持断点续传。
  3. **内容完整性**: 对于所有上传的内容（Blobs 和 Manifests），服务端必须通过 `digest` (SHA256 摘要) 进行校验，确保数据在传输过程中没有损坏。
* **技术与健壮性指标**:

  1. **错误处理**: 服务必须能够处理内部 `panic` 防止意外退出，并为每个接口实现至少 `400` 和 `404` 等标准的 HTTP 错误响应。
  2. **Content-Type 处理**: 必须正确识别并处理客户端发送的 `Content-Type`，特别是对 `manifest` 和 `manifest list` 的多种媒体类型（包括 Docker V2.2 和 OCI 标准）的支持。
  3. **运行环境**: 目标运行环境为 Linux (Debian 13)，代码实现需保证在该环境下稳定运行。
* **内容简化**:

  1. **无认证授权**
  2. **单体文件系统存储**: 采用本地文件系统作为唯一的存储后端，不涉及分布式存储或云存储。
* **核心概念理解**:
  我们认识到 Docker Registry 的本质是一个**内容寻址存储 (Content-Addressable Storage, CAS)** 系统。所有的数据对象（Layers, Configs, Manifests）都以其内容的 SHA256 `digest` 作为唯一标识符进行存储和检索。这个核心理解贯穿了我们整个存储层的设计。

### 2. 系统设计思路

根据网上搜索的资料和Gemini的提示，最终采用了经典的分层架构设计。

* **分层架构**:

  1. **接口层 (Handler)**: 负责处理 HTTP 请求和响应，选用 `gorilla/mux` 库来处理带变量的复杂路由（如 `/v2/{name:.+}/...`）。
  2. **存储层 (Storage)**: 所有文件系统的读、写、移动、删除等底层操作全部封装在这一层，数据库更替无需改动上层的业务逻辑。
  3. **领域模型层 (registry)**: 定义了应用的核心数据结构（如 `Manifest`, `Descriptor`）和业务错误（如 `ErrBlobNotFound`）。这一层是纯粹的业务模型，与具体的实现技术无关。
  4. **中间件层 (Middleware)**: 用于处理跨领域的通用功能，如日志记录、请求追踪和实现的 `panic` 恢复。
* **存储后端设计**:

  1. **目录结构**: 项目严格模仿了官方 Registry 的文件系统布局，以确保兼容性和逻辑清晰性。
     * `blobs/sha256/<xx>/<digest>/data`: 存放所有 Blob 数据。
     * `repositories/<name>/_manifests/tags/<tag>/current/link`: 存放 tag 到 manifest digest 的映射。
     * `repositories/<name>/_uploads/<uuid>/`: 存放进行中的上传会话。
  2. **无状态服务设计**: 所有的状态（如已上传的分片、开始时间）都通过在 `_uploads` 目录下创建临时文件和目录来管理

### 3. 设计与实现流程

采用自底向上、逐步完善的开发流程。

1. **搭建基础**:

   * 首先定义了 `internal/registry` 包中的核心数据结构 (`types.go`) 和错误 (`errors.go`)，确立了系统的基础实体结构
   * 紧接着实现了 `internal/storage/filesystem.go` 中的一些基础操作，在之后的开发中也逐步完善其中功能内容。
2. **构建 API 骨架**:

   * 在 `internal/handler` 包中创建了 `api.go` 来集中注册所有 11 个核心 API 路由，并将它们指向尚未实现的 handler 方法。
3. **逐步实现接口**:

   * 按照 `docker pull` 和 `docker push` 的逻辑顺序，逐一实现了各个 handler 方法。
   * **Manifests API**: 实现了 `GET`, `HEAD`, `PUT`, `DELETE`
   * **Blobs API**: 实现了 `GET` 和 `HEAD`
   * **Blob Uploads API**: 实现了 `POST`, `GET` (状态), `PATCH`, `PUT`, `DELETE` (取消) 的全流程，并支持了跨仓库挂载的优化路径。
4. **组装与加固**:

   * 最后实现了 `internal/middleware/recovery.go`，为服务提供了 `panic` 恢复。
   * 在 `cmd/registry/main.go` 中，我将存储、路由、中间件这些独立的模块组装起来，启动了整个服务。

### 4. 测试思路与验证

* **阶段一：环境搭建与端到端流程测试 (E2E Flow Testing)**:

  1. **目标**: 验证核心用户故事（`docker push` 和 `docker pull`）能否走通。
  2. **工具**: 真实的 `docker` 客户端 (Docker Desktop)。
  3. **环境配置**: 正确配置了 `insecure-registries`，并使用 `host.docker.internal` 解决了 Windows 环境下的网络访问问题。
  4. **核心验证**: 通过推送 `hello-world` 和 `alpine` 镜像，我们验证了整个上传和下载流程是通畅的，并且“Layer already exists”的输出证明了我们的内容寻址和缓存检查 (`HEAD`) 逻辑是正确的。
* **阶段二：自动化与全面 API 测试 (Comprehensive API Testing)**:

  1. **动机**: 我们意识到仅靠端到端测试无法覆盖所有的接口和错误情况（如取消上传、错误处理等）。
  2. **工具**: 编写了 PowerShell 自动化测试脚本，使用 `Invoke-WebRequest` (`curl`) 直接与每一个 API 端点交互。
  3. **核心验证**:
     * **独立验证**: 对 `HEAD /manifests`, `GET /uploads/{uuid}`, `DELETE /uploads/{uuid}` 等在正常流程中不一定会被调用的接口进行了显式测试。
     * **错误路径测试**: 我们特意测试了“按 digest 删除 manifest”应该被我们的服务**正确拒绝**的场景，验证了我们的错误处理逻辑。
     * **状态验证**: 在“取消上传”测试中，我们通过后续的 `GET` 请求返回 `404` 来验证临时资源确实被清理了。
* **阶段三：迭代调试与问题修复 (Iterative Debugging)**:
  测试过程暴露了多个深层次的 Bug，我们通过分析日志、增强测试脚本的错误报告，逐一解决了它们。这个过程是本项目最有价值的部分之一：

  1. **`digest mismatch`**: 定位并修复了因文件系统写缓存导致的竞争条件，通过在 `AppendChunk` 和 `PutManifest` 中强制 `file.Sync()` 解决。
  2. **`unsupported manifest media type`**: 修正了 `Content-Type` 校验逻辑，增加了对现代 Docker 客户端使用的 OCI 媒体类型的支持。
  3. **`Invalid URI` (PowerShell)**: 修复了 Go 程序中 `Location` 头返回相对 URL 的不规范行为，改为返回绝对 URL。
  4. **PowerShell 脚本 Bug**: 解决了 `docker inspect` JSON 数组解析、`curl` 别名以及 `Invoke-WebRequest` 错误处理等多个 PowerShell 特有的陷阱。

通过这三个阶段的测试，我们不仅验证了项目需求的完成度，更最终产出了一个同样健壮的自动化测试套件。
