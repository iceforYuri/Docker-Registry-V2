# 接口实现方式

## Manifest 部分

### GetManifest

* **接口** **:** **GET /v2/{name}/manifests/{reference}**
* **设计与实现对齐**:

  `storage.GetManifest` 函数实现了完整的**引用解析**逻辑，无论是 **tag** 还是  **digest** ，它都能正确地找到目标 manifest。

  * **当引用是 tag 时** ，函数会读取文件系统中对应的 **link** 文件，并解析出它实际指向的  **digest** 。
  * **当引用是 digest 时** ，系统则直接使用这个 digest 进行后续读取。

  **1. 仓库范围验证（关键逻辑）**

  为了与 **DELETE** 操作保持一致，最终版的 `GetManifest` 加入了一个重要的前置检查：

  当用户通过 **digest** 获取 manifest 时，系统会先确认该 digest 的“修订链接”（revision link）是否仍然存在于当前仓库的 `_manifests/revisions/` 目录下。

  如果链接不存在（例如该 manifest 已被删除），就会返回  **404 Not Found** ，即使底层的 blob 数据在全局存储中还在。

  **2. 内容与类型识别**

  找到目标后，函数会从全局 Blob 存储中读取具体内容，并解析其中的 `mediaType` 字段。

  它会根据这个字段动态返回正确的  **Content-Type** ，无论请求的是单架构 manifest 还是 manifest list，客户端都能得到匹配的响应格式。

### PutManifest

* **接口** **:** **PUT /v2/{name}/manifests/{reference}**
* **设计与实现对齐**:

整个注册流程的核心由 `storage.PutManifest` 完成从校验、存储到关联的全部逻辑

**1. 注册与修订记录**

每当有新的 manifest 上传成功，系统都会在 `_manifests/revisions/` 目录里为它建立一份 **不可变的记录** 。

**2. 标签关联**

如果上传时的引用是一个  **tag** （比如 `latest`），系统还会在 `_manifests/tags/` 目录下创建或更新对应的链接文件，用来标明这个 tag 当前指向的 manifest。

**3. 前置校验**

在真正写入存储前，`handleManifestPut` 会先做一系列校验：

它会解析 manifest，找到里面引用的所有 blob（通过 digest 标识），然后逐个确认这些 blob 是否都已经存在于全局存储中。

只要有一个缺失，就会立即中止，并返回 **400 Bad Request** 错误。

**4. 格式与智能维护**

系统还会检查客户端上传的 `Content-Type` 是否是支持的格式（Docker v2.2 或 OCI v1）。

当发现 tag 已经绑定了旧的 manifest 时，它会根据平台信息自动判断是创建新的 manifest list、合并已有的，还是直接替换，确保仓库始终保持一致性和可用性。

### HeadManifest

* **接口** **:** **HEAD /v2/{name}/manifests/{reference}**
* **设计与实现对齐**:

  **1. 存在性验证**

  `handleManifestHead` 直接复用了与 **GET** 请求相同的 `storage.GetManifest` 函数。因此，它同样支持通过 **tag** 或 **digest** 来查询 manifest，并且也继承了 **GET** 的**仓库范围验证**机制

  **2. 轻量级实现**

  在成功调用 `storage.GetManifest` 并获取到完整信息后，处理器会正确设置所有必要的响应头，但不会真正返回内容。

  **3. 元数据返回**

  响应头中包含标准化的 **Docker-Content-Digest** 和 **Content-Length** 信息，提供了客户端进行校验和比对所需的关键元数据。

### DeleteManifest

* **接口** **:** **DELETE /v2/{name}/manifests/{reference}**

首先检查digest合法性

并发控制请求

引用检查：

* 遍历指定仓库下的所有 tag，确认没有任何一个 tag 指向我们正试图删除的 digest。
* 逻辑:
  * 获取到要删除的 digest
  * 定位到该仓库的 tags 目录 ( `<storage-root>`/v2/repositories/`<name>`/_manifests/tags/)。
  * 递归地遍历这个目录下的所有子目录和文件。
  * 对于每一个找到的 link 文件（路径通常是 .../`<tag>`/current/link），读取其内容。
  * 将其内容（这是一个 digest）与我们正要删除的 digest 进行比较。
  * 只要发现任何一个 link 文件的内容匹配，就意味着该 manifest 仍被引用。必须立即停止遍历，释放锁，并向客户端返回 403 Forbidden (或 409 Conflict) 错误。
  * 只有在遍历完所有 tag 且未发现任何匹配的情况下，才能证明该 manifest 是“未被引用的”。

执行删除：

删除仓库级别的元数据链接。在我们的文件系统布局中，这对应的是 manifest 的修订链接 (revision link)，其路径通常是 `<storage-root>`/v2/repositories/`<name>`/_manifests/revisions/sha256/`<digest-hex>`/

## Blob API 接口设计说明

### HeadBlob

* **接口**: `HEAD /v2/{name}/blobs/{digest}`
* **设计与实现对齐**:

  **1. 核心功能：元数据检查**

  此接口的核心职责是高效地验证一个 Blob 是否存在，而不产生实际的数据传输。整个逻辑由 `handleBlobHead` 处理器完成。

  * 它通过调用 `storage.StatBlob(digest)` 将存在性检查委托给存储层。
  * `storage.StatBlob` 函数直接映射到文件系统的 `os.Stat()` 操作，这是获取文件元数据（包括大小和存在性）的最高效方式。

  **2. 成功响应规范**

  当 `storage.StatBlob` 确认 Blob 存在时，`handleBlobHead` 会严格按照规范构建响应：

  * 返回 `200 OK` 状态码。
  * 响应头中必须包含 `Content-Length`（值为 `StatBlob` 返回的文件大小）和 `Docker-Content-Digest`（值为请求的 `digest`）。

  **3. 失败响应规范**

  当 `storage.StatBlob` 确认 Blob 不存在时，它会返回一个特定的 `registry.ErrBlobNotFound` 错误。

  * `handleBlobHead` 会捕获此错误，并精确地返回 `404 Not Found` 状态码，符合接口的错误响应要求。

### ReviseBlob

* **接口**: `GET /v2/{name}/blobs/{digest}`
* **设计与实现对齐**:

  **1. 核心功能：流式下载**

  此接口负责提供 Blob 的完整内容。设计的核心是**性能和低内存占用**。

  * `handleBlobGet` 处理器调用 `storage.GetBlob(digest)`，后者安全地打开文件并返回一个可读的文件句柄 (`io.ReadCloser`)。
  * 处理器随后使用 `io.Copy` 将文件内容从该句柄直接**流式传输**到 HTTP 响应体中。这种设计确保了即使在下载 GB 级别的大文件时，服务的内存消耗也保持在极低水平。

  **2. 响应规范**

  * 在开始传输前，处理器会设置所有必需的响应头，包括 `Content-Length` 和 `Docker-Content-Digest`。
  * `Content-Type` 被明确设置为 `application/octet-stream`，向客户端表明这是一个不透明的二进制数据流。

  **3. 关于 `307 Redirect`**

  暂时没能实现 `307重定向`部分的内容

### PostBlob

* **接口**: `POST /v2/{name}/blobs/uploads/`
* **设计与实现对齐**:

  此接口设计为具有两种截然不同的处理路径，由客户端的查询参数决定。

  **1. 挂载路径 (Mount Path)**

  当请求包含 `mount` 和 `from` 查询参数时，此路径被激活。

  * **前置校验**:
    * **输入校验**: `handleBlobUploadStart` 首先使用正则表达式严格验证 `mountDigest` 的格式，防止非法输入。
    * **权限模拟**: 随后，它调用 `storage.RepositoryExists(fromRepo)` 来验证源仓库是否存在，以此作为权限检查的模拟。
    * **内容校验**: 调用 `storage.StatBlob(mountDigest)` 检查 Blob 是否已在全局存储中存在。
  * **成功逻辑**: 如果所有校验通过，系统会调用 `storage.LinkBlob(repoName, mountDigest)` 在目标仓库的元数据中创建一个**关联记录**（`_layers` 目录下的 link 文件）。随后返回 `201 Created`，`Location` 头指向 Blob 的最终位置，告知客户端无需上传。
  * **失败逻辑**: 任何一步校验失败，都会回退到标准的上传路径。

  **2. 标准上传路径 (Upload Path)**

  这是默认路径，或挂载失败时的回退路径。

  * **会话创建**: `handleBlobUploadStart` 调用 `storage.StartUpload`，后者会在 `_uploads` 目录下创建一个由 UUID 标识的临时目录，用于存放后续上传的数据块。
  * **响应规范**: 返回 `202 Accepted` 状态码。`Location` 头指向这个新创建的、带 UUID 的上传会话 URL。响应头中还必须包含 `Docker-Upload-UUID` 和初始的 `Range: 0-0`。

### GetBlobUpload

* **接口**: `GET /v2/{name}/blobs/uploads/{uuid}`
* **设计与实现对齐**:

  **1. 核心功能：状态查询**

  此接口用于实现**断点续传**。它允许客户端查询一个进行中上传的当前进度。

  * `handleBlobUploadStatus` 处理器调用 `storage.StatUpload`，后者通过检查临时 `data` 文件的大小来获取已接收的字节数。

  **2. 响应规范**

  * 成功时返回 `204 No Content` 状态码，表示所有信息都在响应头中。
  * `Range` 响应头经过特殊处理，以确保其格式在所有情况下（包括 `size=0` 时）都清晰、一致地表示已接收的字节范围（例如 `0-999` 或 `0-0`）。
  * `Location` 和 `Docker-Upload-UUID` 头也会被包含，以提供完整的会话上下文。

### UploadBlobChunk

* **接口**: `PATCH /v2/{name}/blobs/uploads/{uuid}`
* **设计与实现对齐**:

  **1. 前置校验：保证数据连续性**

  这是此接口设计的核心，用于防止数据损坏。

  * 在写入任何数据前，`handleBlobUploadChunk` 处理器会先获取当前已上传的大小 (`currentSize`)。
  * 如果客户端请求中包含 `Content-Range` 头，处理器会严格校验其起始偏移量是否等于 `currentSize`。
  * 如果范围不连续，则返回 `416 Range Not Satisfiable` 错误，明确拒绝该数据块。

  **2. 数据写入与持久化**

  * 校验通过后，处理器将请求体流式传输给 `storage.AppendChunk`。
  * `storage.AppendChunk` 以**追加模式**打开文件，并将数据写入。
  * 写入完成后，调用 `file.Sync()` 将数据从操作系统缓冲区**强制刷新到磁盘**

  **3. 响应规范**

  * 返回 `202 Accepted`，并附带更新后的 `Range` 头，告知客户端新的上传进度。

### CompleteBlob

* **接口**: `PUT /v2/{name}/blobs/uploads/{uuid}?digest=<digest>`
* **设计与实现对齐**:

  **1. 核心功能：最终校验与提交**

  * `handleBlobUploadCommit` 处理器调用 `storage.CommitUpload`。
  * `storage.CommitUpload` 会完整读取临时上传的 `data` 文件，独立计算其 SHA256 `digest`。
  * 计算出的 `digest` 会与客户端在 URL 中提供的 `digest` 参数进行**严格的逐位比较**。
  * 上传实现：
    * 分块上传（chunked）：先 POST 获取上传会话，通过多次 PATCH 上传块，每个块由存储层的追加并 fsync，最后用带 digest 的 PUT 完成。
    * 单次上传（monolithic）：在完成 PUT 请求中可以携带整个 blob 作为最后一个块读取可选 body，并在提交前把它通过 `AppendChunk` 写入临时文件，随后调用存储层校验 digest 并进行原子移动到最终位置。

  **2. 成功逻辑：原子性提交**

  * 如果 `digest` 校验通过，`storage.CommitUpload` 会执行一次**原子性的 `os.Rename` 操作**，将临时文件移动到其在全局 Blob 存储中的最终、不可变的位置。
  * 处理器返回 `201 Created`，`Location` 头指向这个最终位置。

  **3. 失败逻辑**

  * 如果 `digest` 不匹配，则返回 `400 Bad Request`，并附带 `DIGEST_INVALID` 错误码，临时文件将被清理。

### CancelBlob

* **接口**: `DELETE /v2/{name}/blobs/uploads/{uuid}`
* **设计与实现对齐**:

  **1. 核心功能：资源清理**

  * `handleBlobUploadCancel` 处理器调用 `storage.CancelUpload`。
  * `storage.CancelUpload` 的实现非常直接：它对与 `uuid` 关联的整个临时上传目录执行 `os.RemoveAll`，彻底删除所有已上传的分片和元数据。

  **2. 响应规范**

  * 成功删除后，返回 `204 No Content` 状态码，表示操作成功且无内容返回。
